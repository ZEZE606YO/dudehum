# 📋 DUDEHUM — สรุปโปรเจกต์ (สำหรับเปิดแชทใหม่)

> ก๊อปทั้งไฟล์นี้ไปวางในแชทใหม่ เพื่อให้สานงานต่อได้ทันทีโดยไม่ต้องเล่าซ้ำ

สานต่อโปรเจกต์ **DUDEHUM** (เว็บร้านค้าออนไลน์ / marketplace) ที่ `C:\Users\npchi\landing-page`

---

## 🚀 เริ่มงาน
- อ่าน `landing-page/PROJECT_MAP.md` ก่อนเสมอ (สารบัญบอกว่าแต่ละอย่างอยู่ไฟล์ไหน/บรรทัดไหน)
- เปิด/ปิด/รีสตาร์ทเว็บ: `powershell -ExecutionPolicy Bypass -File landing-page/webctl.ps1 start|stop|restart|status`
- รันที่ `http://localhost:5173` (Node เสิร์ฟทั้ง static + API origin เดียว)
- แก้ไฟล์ `html/css/js` = เห็นผลทันที (กันแคชด้วย no-cache แล้ว) | แก้ `server/*.js` = ต้อง **restart**
- Stack: Node.js built-in ล้วน (ไม่มี npm package), vanilla JS, ข้อมูลเก็บเป็น JSON ใน `server/data/` (gitignored — อย่าแก้มือ)
- อยู่บน GitHub: **ZEZE606YO/dudehum**

## 🎨 กติกาดีไซน์ (ห้ามลืม)
- **ขาว–ส้มเท่านั้น** ทุกหน้า ไม่มี dark mode
- **ห้ามอีโมจิ** ใช้ไอคอนเส้น SVG `<svg class="icon">`
- ชื่อในข้อความ = **"DUDEHUM"**
- ฟอนต์ IBM Plex Sans Thai (เนื้อหา) + Sora (หัวข้อ)

---

## ✅ ที่ทำเสร็จแล้ว (ใช้งานได้จริง เทสผ่าน)

**ระบบหลัก**
- Auth: สมัคร + OTP (อีเมลจริงผ่าน Brevo), login, ลืม/รีเซ็ตรหัส
- ร้านค้า: ลงขายสินค้าเอง, หมวดหมู่, ตะกร้า, ถูกใจ, ออเดอร์, หน้ารายละเอียดสินค้าสไตล์ Shopee
- แชท: หน้าแชทเต็ม (`chat.html`) polling ทุก 4 วิ + เริ่มแชทจากสินค้าได้
- หน้าบัญชี (`account.html`): ประวัติออเดอร์ + ตั้งค่าพร้อมเพย์

**แผงแอดมิน (`admin.html`)** — หน้าเดียว 5 แท็บ: ภาพรวม / สมาชิก / ออเดอร์ / สินค้า / รายงาน
- แอดมิน = อีเมล `np.chinnakit@gmail.com` คนเดียว (เช็ก `isAdmin` ที่ server ทุก `/api/admin/*`)
- **จัดการสมาชิก**: ตาราง + ค้นหา, ดูรายละเอียดรายคน (บัญชี/สินค้า/ออเดอร์/สรุปแชท), แบน, ลบถาวร
  - แบน = login ไม่ได้ + หลุดทุกหน้า (ผ่าน `sessionUser`) | ลบ = ลบบัญชี + สินค้า + รูป, เก็บออเดอร์ไว้
- **รายงาน**: ผู้ใช้กด "รายงานปัญหา" ในแชท เลือกหมวด 6 อย่าง → แอดมินเห็นเฉพาะห้องที่ถูกรายงาน แสดงเป็น**หน้าแชท** (บับเบิลซ้าย-ขวา ผู้รายงานฝั่งขวา)
- คำเตือน "DUDEHUM เป็นคนกลาง" โผล่ในห้องแชทก่อนเริ่มสนทนา
- ลิงก์ "แผงแอดมิน" ในเมนูผู้ใช้ `shop.html` โผล่เฉพาะแอดมิน

**นโยบายความเป็นส่วนตัว**: แอดมินอ่านแชทได้เฉพาะห้องที่ถูกรายงาน (ไม่แอบอ่านทุกห้อง — ตัดสินใจร่วมกันเพราะ PDPA/จริยธรรม)

---

## 🔜 ที่อยากทำต่อ
1. แบนแล้ว **"ซ่อนสินค้า"** ของคนนั้นออกจากร้านด้วย (ตอนนี้แบนแค่ login ไม่ได้ สินค้ายังโชว์)
2. ปุ่ม **"ระงับผู้ถูกรายงาน"** ในหน้าอ่านรายงาน (แบนคนผิดได้เลยในหน้าเดียว)
3. แก้คำสะดุด **"กดว่าจะกลับมาซื้ออีก"** ใต้ตัวเลข 98% ในหน้าแรก (`index.html`)
4. เพิ่มลิงก์ "แผงแอดมิน" ในหน้า `account.html` ด้วย

## ⏸️ พักไว้ (ยังไม่ทำจนกว่าจะพร้อม)
- **ระบบจ่ายเงินพร้อมเพย์ + ความปลอดภัย** — จุดอ่อนที่รู้แล้ว: ปุ่ม "ยืนยันว่าโอนแล้ว" ผู้ซื้อกดเองคนเดียว เชื่อใจล้วน ไม่ verify จริง

---

## ⚠️ ข้อควรระวัง (บั๊กที่เคยเจอ)
- attribute `hidden` จะไม่มีผลถ้ามี CSS `.class` ตั้ง `display` ทับ → ต้องใส่ `[hidden]{display:none!important}`
- อย่าแก้ไฟล์ใน `server/data/` ด้วยมือ (`users.json`/`orders.json` ฯลฯ) ให้แอปจัดการเอง
